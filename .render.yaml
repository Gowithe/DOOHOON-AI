services:
  - type: web
    name: doohoon-linebot
    env: docker
    plan: free
    branch: main
    rootDir: .
    buildCommand: ""
    startCommand: "php -S 0.0.0.0:10000 -t ."
    envVars:
      - key: PORT
        value: 10000
